# Generated by scripts/schema-gen.ts and compared byte-for-byte by
# `npm run schema:check` (schema-gen.ts's --check does a string comparison
# against the committed file). Reformatting these breaks that check with no
# underlying schema change.
schemas/

# Written by the ingest persist layer (ADR-011 semantic-diff). Reformatting
# fights the semantic-change detection and creates ingest-diff noise.
public/data/

# Captured HTML/JSON responses used as golden fixtures (npm run
# fixture:verify, scripts/fixture-verify.ts). Extraction asserts exact
# selector/whitespace behavior against the real captured bytes --
# reformatting silently breaks that without touching the script itself.
fixtures/

dist/
node_modules/
.wrangler/
package-lock.json
