# 05 — Source Inventory

**Status: Q1 resolved, populated 2026-09-12; M2a breadth added 2026-09-13; M2b added a tenth; M4b (2026-09-16) deepened six targets' content; M5a/M5b (2026-09-17) added an eleventh and twelfth, closing the source-quality gate (every section now has ≥1 substantive, non-numeric source); M6a (2026-09-17, folding in M4c) retired `bluecore-form-d` and converted `bluecore-newsroom` to a 3-item composite list (ADR-025), landing at eleven.** Bluecore Energy is a real target, literally — see §6 item 2. Eleven sources triaged across all four sections (seven at Q1, `nuscale-sec-filings` and `eia-ca-industrial-price` at M2a, `bluecore-sec-filings` at M2b, `usaspending-advanced-reactor-awards` at M5a, `fr-maritime-reactor` at M5b, minus `bluecore-form-d` at M6a); the table below is real, not illustrative.

This document is the real specification. `SPEC.md` describes a machine; this describes its fuel.

---

## 1. Why this comes before code

Two things here can invalidate architectural decisions already made:

**PDF share.** If more than roughly 30% of sources are PDFs, "Cheerio primary, Playwright fallback" is the wrong stack constraint, and the `pdf` kind — currently a sketch in `02-CONFIG-SCHEMA.md` §7 — becomes a first-class design problem rather than a deferred one. Scanned PDFs needing OCR raise a sharper question: OCR has an error rate, which sits awkwardly next to a determinism guarantee. That may need to be a labelled, lower-confidence class of block rather than a normal one.

**Session-state applications.** Government dockets frequently live in older frameworks with server-side session state, postback pagination, and no addressable URLs per record. These are not "use Playwright" problems — they are "the deep link you want does not exist" problems, and they change what is extractable at all.

Finding either out during implementation costs a rewrite. Finding out during triage costs an afternoon.

---

## 2. Triage columns

| Column         | Values                                                                 | Why it matters                                  |
| -------------- | ---------------------------------------------------------------------- | ----------------------------------------------- |
| `id`           | slug                                                                   | Becomes `targetId` and the data filename        |
| Section        | company / competitive / regulatory / market                            | Grid placement                                  |
| URL            | absolute                                                               | The source                                      |
| Type           | static-html / js-rendered / json-api / pdf / pdf-scanned / session-app | Drives `kind` and `renderer`                    |
| Volatility     | realtime / daily / weekly / monthly / quarterly / episodic             | Drives `cron` and `ttlHours`                    |
| Auth           | none / key / login                                                     | Login sources need a separate risk conversation |
| Robots         | allow / disallow / unclear                                             | Q6 input                                        |
| Anchor quality | stable-id / class / positional / text-only                             | Predicts breakage rate                          |
| Facts          | count                                                                  | Extractors on this target                       |
| Feasibility    | easy / moderate / hard / blocked                                       | Build order                                     |
| Notes          | prose                                                                  | The fragile parts                               |

**Anchor quality** is the maintenance predictor. A source with stable `id` attributes may never break. A table matched by `nth-child` will break the first time a row is inserted. Triaging this up front tells you where the weekly cost will land, and lets you drop a source that is more trouble than the fact is worth.

---

## 3. Inventory

Entity `bluecore-energy` (role: primary) is BlueCore Energy, Inc. — SEC CIK [0002125928](https://www.sec.gov/cgi-bin/browse-edgar?action=getcompany&CIK=0002125928), DE-incorporated, HQ at Berth 48, Port of Long Beach, CA. Building a ~10 MWe floating water-cooled SMR for maritime/port power; emerged from stealth July 2026; $50M seed closed September 2026 (Silverton Partners). Entity `oklo-inc` (role: competitor) is Oklo Inc. (NYSE: OKLO, CIK 0001849056) — the closest publicly-traded advanced-fission peer, used as the Competitive Landscape "competitor activity" source. Entity `nuscale-power` (role: competitor), added M2a, is NuScale Power Corporation (NYSE: SMR, CIK 0001822966) — the other publicly-traded advanced-fission peer, previously named as an equally-viable second competitor but deferred at Q1.

### Section: Company Performance

| id                     | URL                                                                                                    | Type        | Volatility                                         | Auth | Robots                                                                      | Anchor                                                                                                  | Facts | Feasibility | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------------------- | ------------------------------------------------------------------------------------------------------ | ----------- | -------------------------------------------------- | ---- | --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ----- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `bluecore-sec-filings` | [data.sec.gov/submissions/CIK0002125928.json](https://data.sec.gov/submissions/CIK0002125928.json)     | json-api    | episodic, same cadence as the filer above          | none | allow (same `data.sec.gov` posture as oklo-sec-filings/nuscale-sec-filings) | stable-id (documented, versioned JSON field path)                                                       | 3     | easy        | Added M2b. SEC's submissions JSON — the sole source for this filer's SEC activity as of M6a (`bluecore-form-d`, a target pinned to one specific accession number, was retired then). M4b added `latest_filing_summary`, a composite linking to the latest filing's primary document — a generic composite over row 0, not an 8-K extractor, since this filer's own history is two Form D filings and no 8-K.                                                                                                                                                                                                                                                                                                                              |
| `bluecore-newsroom`    | [bluecore.energy/news-insights](https://www.bluecore.energy/news-insights)                             | static-html | weekly (9 posts/press mentions Aug 10 – Sep 11)    | none | allow (no robots.txt restrictions at all)                                   | class (hand-authored `bc-n-*` classes; Webflow UUID `id`s nearby are unstable — do not anchor on those) | 2     | easy        | Use `:first`/`:lt(3)`, **not** `:first-child`/`:nth-child` — each card sits in its own individual Webflow collection-list wrapper (`div.w-dyn-item`), so every card is trivially the first child of its own parent and `:first-child` matched all 12 (caught live, via the dry run). Also avoid the `.bc-n-feat-*` hero block — it's manually pinned to the July stealth-launch post and does not advance. M6a (ADR-025) converted the single "latest post" scalar into `recent_posts`, a 3-row composite list (`.bc-n-card:lt(3)` + `limit: 3`) whose row _is_ the `<a class="bc-n-card">` — `url` reads its `href` directly (mixed relative/absolute, resolved via `escape: "href"`), while `title`/`category`/`date` are its children. |
| `bluecore-careers`     | [api.lever.co/v0/postings/bluecore-energy](https://api.lever.co/v0/postings/bluecore-energy?mode=json) | json-api    | weekly (9 postings created Aug 10 – Sep 10, ~2/wk) | none | allow (`Allow: /`, `Crawl-delay: 1`)                                        | stable-id (documented public API)                                                                       | 1     | easy        | Open-roles count as a real-time headcount-growth proxy. `jsonPath: "$.length"` — jsonpath-plus's array-length pseudo-property, verified against the live payload (returns `[9]`, single match). Respect `Crawl-delay: 1` via `politeness.minIntervalMs`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

### Section: Competitive Landscape

| id                    | URL                                                                                                                                                             | Type     | Volatility                                                                                       | Auth | Robots                                                                                                                                                                          | Anchor                                            | Facts | Feasibility | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------ | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- | ----- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `oklo-sec-filings`    | [data.sec.gov/submissions/CIK0001849056.json](https://data.sec.gov/submissions/CIK0001849056.json)                                                              | json-api | realtime/daily (multiple Form 4/144/8-K filings per week, several in the days before triage)     | none | allow (no robots.txt at `data.sec.gov` → 404 → unrestricted; still honor SEC's Fair Access policy: declared UA with contact info, ≤10 req/sec — a daily cron is far under this) | stable-id (documented, versioned JSON field path) | 3     | easy        | **Competitor activity**, literally per `SPEC.md` §3's own definition of this section. Oklo (NYSE: OKLO) is the closest publicly-traded peer at a comparable "advanced reactor, pre-revenue-but-scaling" stage. NuScale (CIK 0001822966) is an equally-viable second competitor — added below, M2a. M4b added `latest_8k`: a composite/indexed extractor (ADR-022) binding the most recent 8-K's row across `filings.recent`'s parallel arrays, rendering its item codes as plain-English labels and linking to the primary document.                                                                 |
| `nuscale-sec-filings` | [data.sec.gov/submissions/CIK0001822966.json](https://data.sec.gov/submissions/CIK0001822966.json)                                                              | json-api | realtime/daily (identical filing cadence to Oklo — most recent at triage a 4/A dated 2026-09-11) | none | allow (same data.sec.gov posture as oklo-sec-filings)                                                                                                                           | stable-id (documented, versioned JSON field path) | 3     | easy        | Second competitor, added M2a — previously deferred at Q1 (see the oklo-sec-filings note above). Identical shape to oklo-sec-filings by design: same API, same facts (`latest_filing_form`/`latest_filing_date`, plus M4b's `latest_8k`), same Fair Access politeness.                                                                                                                                                                                                                                                                                                                                |
| `fr-smr-mentions`     | [federalregister.gov API, term="small modular reactor"](https://www.federalregister.gov/api/v1/documents.json?conditions%5Bterm%5D=%22small+modular+reactor%22) | json-api | weekly                                                                                           | none | allow (API path not in `/robots.txt` disallow list; only `/documents/search` etc. are blocked)                                                                                  | stable-id (documented `count` field)              | 5     | easy        | Sector-wide document volume as a "sector capacity metrics" proxy, distinct from the NRC-specific regulatory count below (76 vs. 55 documents at triage time — the ~21 document gap is exactly the non-NRC agencies, e.g. DOE, discussing SMRs). No `expectMonotonic` — FR's index can in principle be revised down, so this isn't a hard invariant. M4b added `latest_document_title` (a composite link), `latest_document_type` (enum, live-verified five values), `latest_document_date`, and — only here, since the other two FR targets already filter to one agency — `latest_document_agency`. |

### Section: Regulatory & Policy

| id                    | URL                                                                                                                                                                                                                                                                          | Type     | Volatility                                         | Auth | Robots | Anchor    | Facts | Feasibility | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------- | ---- | ------ | --------- | ----- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `fr-nrc-smr`          | [federalregister.gov API, agency=nuclear-regulatory-commission + term="small modular reactor"](https://www.federalregister.gov/api/v1/documents.json?conditions%5Bagencies%5D%5B%5D=nuclear-regulatory-commission&conditions%5Bterm%5D=%22small+modular+reactor%22)          | json-api | weekly                                             | none | allow  | stable-id | 4     | easy        | NRC-specific regulatory-attention proxy for the SMR class Bluecore competes in. Bluecore itself has **no NRC docket yet** — the Long Beach barge holds no fuel and is pre-certification (confirmed via news coverage) — so this is a sector-level regulatory signal, not a Bluecore-specific docket. Revisit once/if a Bluecore-specific NRC docket opens. M4b added `latest_document_title`/`_type`/`_date`, same shape as `fr-smr-mentions`.                                                                                                                                                                                                                                                                                                                                                                                                              |
| `fr-doe-nuclear`      | [federalregister.gov API, agency=energy-department + term="advanced nuclear"](https://www.federalregister.gov/api/v1/documents.json?conditions%5Bagencies%5D%5B%5D=energy-department&conditions%5Bterm%5D=%22advanced+nuclear%22)                                            | json-api | weekly/monthly                                     | none | allow  | stable-id | 4     | easy        | DOE policy-attention document count — "policy incentives" is regulatory signal, not a macro indicator, so this target lives here rather than under Market Conditions (moved 2026-09-13; originally triaged under the section then called "Market Climate"). M4b added `latest_document_title`/`_type`/`_date` — this query's own live result set is where the rare "Uncategorized Document" `type` value was observed.                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `fr-maritime-reactor` | [federalregister.gov API, agencies=coast-guard+maritime-administration + term=reactor](https://www.federalregister.gov/api/v1/documents.json?conditions%5Bagencies%5D%5B%5D=coast-guard&conditions%5Bagencies%5D%5B%5D=maritime-administration&conditions%5Bterm%5D=reactor) | json-api | weekly (15 documents at triage, newest 2026-05-07) | none | allow  | stable-id | 5     | easy        | Added M5b — closes the roadmap's maritime-regulatory gap (MARAD/Coast Guard). `term=reactor` chosen over `nuclear` (185 documents, dominated by unrelated marine security zones) and a quoted `"small modular reactor"` (only 1 document, too narrow to ever move again); newest at triage a 2026-05-07 MARAD RFI directly on point for BlueCore's floating-power-plant concept. Same shape as `fr-nrc-smr`/`fr-doe-nuclear`/`fr-smr-mentions`; includes `latest_document_agency` (like `fr-smr-mentions`, unlike the single-agency FR targets) since this query spans two agencies. That extractor's jsonPath is `agencies[-1:]`, not `[0]` — live-verified both agencies here are listed under a parent department first (e.g. `[Transportation Department, Maritime Administration]`), a fix also backported to `fr-smr-mentions` in the same milestone. |

### Section: Market Conditions

| id                                    | URL                                                                                                          | Type     | Volatility                                                                                                                | Auth                                   | Robots                                                                                                                                                            | Anchor                                                                                              | Facts | Feasibility | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | ----- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `eia-ca-industrial-price`             | [api.eia.gov/v2/electricity/retail-sales](https://api.eia.gov/v2/electricity/retail-sales/data/)             | json-api | monthly (EIA's retail-sales series runs ~2-3 months behind real time)                                                     | key (`EIA_API_KEY`, free registration) | allow (every route requires the key, including metadata discovery — confirmed live, no unauthenticated path exists to check robots-equivalent posture separately) | stable-id (documented, versioned JSON field path)                                                   | 2     | easy        | Added M2a — previously deferred at Q1 (`api.eia.gov` 403s on every route without a key, confirmed again live 2026-09-13). California industrial retail price is the regional/sector cut most relevant to a Long Beach port operator. First (and so far only) authenticated target in this config — exercises `auth`/`secretEnv` for real. `price_period` is kept a plain string (`"YYYY-MM"`), not `type: "date"` — it has no day component.                                                                                                                                                                                                                                                                                                              |
| `usaspending-advanced-reactor-awards` | [api.usaspending.gov spending_by_award search](https://api.usaspending.gov/api/v2/search/spending_by_award/) | json-api | roughly monthly (43 contracts total at triage across the keyword filter, ~6 in the trailing 12 months, newest 2026-08-27) | none                                   | allow (`api.usaspending.gov/robots.txt` → 404, unrestricted — same convention as `data.sec.gov`)                                                                  | stable-id (documented JSON field names; bracket-syntax jsonPath needed for names containing spaces) | 4     | easy        | Added M5a. Roadmap's own filter (SAM.gov, NAICS `221113` + California) returns **zero** contracts since 2007; `api.sam.gov` needs a registered key capped at 10 req/day with no documented sort. Substituted USAspending's `spending_by_award` search, keyless and sortable, filtered nationwide on keywords (`small modular reactor`/`microreactor`/`advanced reactor`). First `method: "POST"` target in this config — its own POST-only endpoint 405s on a plain `GET`, which would have left the dashboard link dead; resolved via ADR-024's new `viewUrl` field (rule 14: required whenever `method: "POST"`). Extracts `latest_award` (composite: recipient, description, link), `latest_award_amount`, `latest_award_agency`, `latest_award_date`. |

`fr-doe-nuclear` previously sat in this section under its old "Market Climate" framing but was reclassified as regulatory signal (see the Regulatory & Policy table above).

---

## 4. Summary

| Metric             | Count                                     | Decision it drives                                                                                                                          |
| ------------------ | ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| Total sources      | 11                                        | Well under any scale ceiling; maintenance cost is low                                                                                       |
| static-html        | 1 (`bluecore-newsroom`)                   | Baseline — server-rendered, no Playwright needed                                                                                            |
| js-rendered        | 0                                         | No `renderer: "browser"` targets needed for this set                                                                                        |
| json-api           | 10                                        | Cheapest and most durable — the strong majority, as hoped                                                                                   |
| pdf / pdf-scanned  | 0 (0%)                                    | **Well under 30%** — "Cheerio primary, Playwright fallback" stands; no PDF handler needed for this set (see §5 flag below on Form D format) |
| session-app        | 0                                         | None found; no infeasible sources                                                                                                           |
| Requiring auth     | 1 (`eia-ca-industrial-price`)             | First real exercise of `auth`/`secretEnv` — added M2a, see the Market Conditions table above                                                |
| `method: "POST"`   | 1 (`usaspending-advanced-reactor-awards`) | First target to use `HttpFetcher`'s POST/body path for real (M5a) — surfaced the provenance-link gap ADR-024's `viewUrl` field resolves     |
| robots: disallow   | 0                                         | Q6 — no blocked sources in this set; nothing to resolve                                                                                     |
| Positional anchors | 0                                         | No `nth-child`-style anchors used; predicted weekly breakage from this set is near zero                                                     |

**PDF flag (per task instructions, not built here):** none of the 9 triaged sources are PDFs — the SEC Form D is XML, not PDF, and cheerio parses it correctly as a lenient HTML-ish document (verified: tag-name selectors like `totalOfferingAmount` resolve as expected). If a future source turns out to need a `pdf` kind, that's new-handler work per ADR-010/`02-CONFIG-SCHEMA.md` §7 and should be flagged before building, not assumed in scope.

---

## 5. Method

Per candidate source, fifteen minutes:

1. `curl` it. Does the fact appear in the raw HTML? If yes, it's `static-html` and easy.
2. If not, open devtools. Is there an XHR returning JSON? **Scrape that instead of the DOM** — an internal JSON endpoint is more stable than the page rendering it and is usually the single biggest durability win available.
3. If neither, it's `js-rendered` and needs Playwright. Note the cost.
4. Inspect the anchor. Stable `id`? A `data-` attribute? Or `nth-child` against an unlabelled table?
5. Check `/robots.txt`.
6. Estimate volatility from the page's own timestamps, not from what you'd like the cadence to be.
7. Write the `notes` field now, while you have the page open.

**Prefer the boring source.** A government JSON endpoint that updates weekly beats a beautiful dashboard that updates hourly behind a React SPA. Durability is worth more than freshness for most of these facts, and every avoided Playwright target is CI minutes and a class of breakage you never deal with.

---

## 6. What to hand back

1. **This table, populated.** Done — see §3/§4 above, seven sources across all four sections.

2. **A one-line answer on Bluecore Energy.** Real target, literally — confirmed via SEC EDGAR (CIK 0002125928, BlueCore Energy, Inc., DE-incorporated), its own domain `bluecore.energy`, and independent press (TechCrunch, Bloomberg, Axios, Fortune, LA Times) around its July 2026 stealth launch and September 2026 $50M seed. Nothing here is throwaway.

3. **The three facts that matter most to the reader.** Proposed, per the "I'll propose them from triage" answer:
   - **Latest funding milestone** (`bluecore-form-d`: `totalOfferingAmount`, `totalAmountSold`) — the single fact a deal-team reader cares most about, and one where the _official_ filing already disagrees with the _press_ headline ($21.8M offered / $19.4M sold vs. the widely-reported "$50M raise"). That gap is exactly the kind of noise-cutting `SPEC.md` §1 says this tool exists to do.
   - **Latest headline + date + category** (`bluecore-newsroom`, three extractors on one page) — the freshness/"what's happening now" signal, and a clean architectural fit: one target, one section, three extractors, real anchors, weekly cadence. **This is the recommended M1 walking-skeleton target** — it satisfies the roadmap's "one target, two or three extractors, one section" scope discipline better than any other candidate here (Form D updates too rarely to demo a freshness badge changing state; the newsroom updates about weekly).
   - **Open-roles count** (`bluecore-careers`) — a cheap, durable, genuinely volatile hiring-velocity proxy or growth signal that a JSON API makes nearly free to maintain.

   `oklo-sec-filings` (competitor activity) is a strong fourth candidate for M2 breadth once M1's single target is proven end to end, since Competitive Landscape's whole point per `SPEC.md` §3 is competitor activity, not just Bluecore's own state.
